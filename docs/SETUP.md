# Setup

1. Install Node.js 18 or newer.
2. Run `npm install`.
3. Copy `.env.example` to `.env`.
4. Fill in Supabase, Stripe, and OpenAI values.
5. Run `npm run dev` and open `http://localhost:3000`.

Required checks:

- `npm run lint`
- `npm run typecheck`
- `npm run build`
